Two additional functions are used for this assignment:


featureReduction:
Detects indices of features that provides no value to learning. Pixels that are same in all the images are removed and the new features array is returned


get_dummies :
An alternative to get_dummies function of pandas. It used for generating a one-of-K encoded array from a single dimensional label array


